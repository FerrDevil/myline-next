export async function generateMetadata({ params : {lang}}){
  return {
    title: lang === "ru" ? 'Выберите доктора': "Select a doctor",
    description: 'Generated by create next app',
  }
}

export default function Layout({children}) {
  return (
    <>
      {children}  
    </>    
  )
}